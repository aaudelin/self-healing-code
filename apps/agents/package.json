{
  "name": "@aiops/agents",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn src.main:app --reload --host 0.0.0.0 --port 4100",
    "build": "uv sync",
    "start": "uv run uvicorn src.main:app --host 0.0.0.0 --port 4100",
    "lint": "uv run ruff check .",
    "format": "uv run ruff format .",
    "test": "uv run pytest",
    "clean": "rm -rf .venv .pytest_cache .ruff_cache node_modules .turbo"
  }
}
